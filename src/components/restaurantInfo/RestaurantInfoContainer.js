import React from "react";

const RestaurantInfoContainer = ({ children }) => {
  return <div className="restaurant-detail-info-container">{children}</div>;
};

export default RestaurantInfoContainer;

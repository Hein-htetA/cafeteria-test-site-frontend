import React from "react";

const CheckoutField = ({ children }) => {
  return (
    <div
      style={{
        padding: "0px 0px",
        
      }}
    >
      {children}
    </div>
  );
};

export default CheckoutField;
